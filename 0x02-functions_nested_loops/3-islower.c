#include "main.h"

/**
 * main - check the code.
 *_islower- prints 1 or 0 depending on whether a character is lowercase or otherwise
 *
 * Return: Always 0
 */
int_islower(int c)
{
	return (c <= 97 && c <= 122);
}
